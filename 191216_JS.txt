` 자바 스크립트는 변수 선언 및 초기화 시에 let을 앞에 붙여준다
` Vanilla JS는 모든 브라우저에 들어가 있기에 다운로드를 따로 할 필요가 없다. 그것이 장점.
` const는 상수로 변하지 않음. var는 변수로 3년전?에 사용하던 거라함. 결국 let과 const를 사용하면됨.
` 첫 사용은 const로 필요하면 let로
` array는 const 배열명 = [내용] 처럼 사용, 다른 데이터 타입을 사용해도 된다
` Object는 { 중괄호 } 내에  ' 변수 : 내용 ', 형식으로 사용하면 됨
` Object 내의 변수 호출시 오브젝트.변수 형식으로 사용
` ( , ) 콤마의 사용 중요 / string은 " " 쌍따옴표로 묶어주기
` 백틱( ` )으로 따옴표를 대신하여 쓸 경우, ${매개변수} 형태로 매개변수를 사용할 수 있다
` 객체 내에 함수를 만들어 사용할 수 있다. 